---
cargo/mitsuro: patch (Added)
---

Hive is now a third mode: persistent named Workers with private DMs, Groups with workbench/roundtable/direct turns, isolated Worker memory, Calendar schedules that target a Worker or a Group, and always-on heartbeat wakes. Chat and Code are unchanged.
