---
cargo/mitsuro: patch (Changed)
---

Releases now use Sampo changesets for version bumps, changelogs, and `v{version}` tags. The existing binary pipeline still builds CLI, Hive, and desktop artifacts on that tag.
