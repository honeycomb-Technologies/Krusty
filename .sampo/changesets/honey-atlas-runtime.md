---
cargo/mitsuro: patch (Fixed)
---

Honey finds the Atlas `agent-browser` sidecar shipped in the linux archive after install, safely adopts exact predecessor user-systemd units into managed release links, verifies downloaded repair archives before activation, and rejects malformed release tags before constructing download or dispatch targets.
