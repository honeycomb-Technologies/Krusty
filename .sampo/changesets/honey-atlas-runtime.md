---
cargo/mitsuro: patch (Fixed)
---

Honey finds the Atlas `agent-browser` sidecar shipped in the linux archive after install, instead of returning API 503 and telling operators to run a source-only script.
