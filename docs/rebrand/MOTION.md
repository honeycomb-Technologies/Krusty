# Mitsuro motion

The primary identity transition is a simultaneous six-side trace of the canonical cell. Each side includes its adjacent rounded corner, so the six open paths reconstruct the exact closed mark together. The trace completes in 42 frames at 60 fps; the graphite well fades in from frames 28–48. Total composition length is 1.6 seconds.

Motion is one-shot, quiet, and structural. Respect reduced-motion settings in interactive app components; reduced motion should show the completed mark without tracing. Avoid perpetual logo rotation, bouncing, or honey-themed novelty motion.
