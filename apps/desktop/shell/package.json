{
  "name": "@krusty/desktop-shell",
  "version": "0.8.2",
  "private": true,
  "packageManager": "bun@1.3.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --bundles deb,rpm",
    "build:linux": "tauri build --bundles deb,rpm",
    "build:all": "tauri build",
    "build:appimage": "bash -lc 'command -v linuxdeploy >/dev/null 2>&1 || { echo \"linuxdeploy is required for AppImage builds.\"; exit 1; }; tauri build --bundles appimage'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5"
  }
}
