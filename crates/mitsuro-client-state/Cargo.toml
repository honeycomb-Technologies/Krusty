[package]
name = "mitsuro-client-state"
version = "0.10.1"
publish = false
edition = "2021"
description = "Headless Rust state machine for Mitsuro clients"

[dependencies]
mitsuro-client = { path = "../mitsuro-client" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints]
workspace = true
